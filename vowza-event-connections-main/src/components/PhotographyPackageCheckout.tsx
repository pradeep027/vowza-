import { useState } from 'react'; import { ArrowLeft, CheckCircle2 } from 'lucide-react'; import { useNavigate } from 'react-router-dom'; import { supabase } from '@/integrations/supabase/client'; import { toast } from 'sonner';
export default function PhotographyPackageCheckout({payload}:{payload:any}){const nav=useNavigate();const [date,setDate]=useState(new Date().toISOString().slice(0,10));const [time,setTime]=useState('');const [venue,setVenue]=useState('');const [notes,setNotes]=useState('');const [busy,setBusy]=useState(false);const book=async()=>{setBusy(true);const {error}=await supabase.rpc('create_photography_package_booking' as any,{p_package_id:payload.packageId,p_event_date:date,p_event_time:time||null,p_venue:venue||null,p_notes:notes||null,p_addon_ids:payload.addons});setBusy(false);if(error){toast.error(error.message);return}sessionStorage.removeItem('vowza_photography_checkout');toast.success('Photography booking requested.');nav('/my-bookings')};return <div className="min-h-screen bg-background p-4 md:p-8"><div className="mx-auto max-w-2xl"><button onClick={()=>nav(-1)} className="mb-5 text-sm"><ArrowLeft className="mr-1 inline h-4"/>Back to packages</button><div className="rounded-2xl border bg-white p-6"><h1 className="text-2xl font-bold">Photography package checkout</h1><p className="mt-1 text-sm text-muted-foreground">{payload.packageName} · {payload.providerName}</p><div className="mt-6 space-y-4"><label className="block text-sm font-medium">Event date<input type="date" min={new Date().toISOString().slice(0,10)} value={date} onChange={e=>setDate(e.target.value)} className="mt-1 w-full rounded-xl border p-3"/></label><label className="block text-sm font-medium">Event time<input value={time} onChange={e=>setTime(e.target.value)} placeholder="e.g. 6:00 PM" className="mt-1 w-full rounded-xl border p-3"/></label><label className="block text-sm font-medium">Venue<input value={venue} onChange={e=>setVenue(e.target.value)} className="mt-1 w-full rounded-xl border p-3"/></label><label className="block text-sm font-medium">Notes<textarea value={notes} onChange={e=>setNotes(e.target.value)} className="mt-1 w-full rounded-xl border p-3" rows={3}/></label><button disabled={busy} onClick={book} className="w-full rounded-xl bg-[#8B1538] py-3 font-semibold text-white disabled:opacity-50"><CheckCircle2 className="mr-1 inline h-4"/>{busy?'Booking…':'Confirm photography booking'}</button></div></div></div></div>}
